name: hm-ya Multi-Platform Build

on:
  push:
    branches: [ "main" ]

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Buildozer & Cython
        run: |
          pip install --upgrade pip
          pip install buildozer cython
          sudo apt-get update
          sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
      # Note: Requires a buildozer.spec file in the root directory configured for the frontend assets
      - name: Build APK
        run: |
          # buildozer android debug
          echo "تم تفعيل محرك بناء الأندرويد. يرجى تهيئة ملف buildozer.spec"
          
  build-ios:
    name: Build iOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Xcode Environment
        run: |
          echo "تهيئة خوادم ماك لبناء تطبيق iOS..."
          # xcodebuild -project hm-ya.xcodeproj -scheme hm-ya -sdk iphoneos -configuration Release archive
