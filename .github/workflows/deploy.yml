name: Deploy to Railway
        on: [push]
        jobs: { deploy: { runs-on: ubuntu-latest, steps: [{ uses: actions/checkout@v2 }, { name: Deploy, run: "npm install -g @railway/cli && railway up" }] } }